import { Playball, Forum } from "next/font/google";

export const playballFont = Playball({
    weight: '400',
    subsets: ['latin'],
})

export const forumFont = Forum({
    weight: '400',
    subsets: ['latin'],
})